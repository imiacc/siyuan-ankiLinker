# Changelog

## 0.1.5

- 优化面板按钮布局：保留顶部主操作按钮尺寸与文案，同时整体右对齐。
- 将“思源闪卡状态”区的诊断/刷新/清理按钮移动到标题行右侧，减少纵向占用并统一布局风格。
- 将“同步预览”区的预览/同步按钮移动到标题行右侧，与其他折叠/操作区布局保持一致。
- 为路径规则摘要视图补充编辑/完成交互，并更新中英文界面文案。
- 更新中英文 README，补充当前安装、同步标签识别与兼容策略说明。
- 移除对旧 Anki 标签 `ankiLinker` 的查询与写入支持，后续仅使用当前插件标签。


## 0.1.4

- 修复闪卡来源聚合逻辑，改为合并运行时缓存、Riff API、到期卡、SQL 与制卡块扫描结果，而非“命中首个来源即返回”。
- 增强卡片去重与身份迁移处理：按 `blockID` 合并来源，优先保留真实 `cardID`，兼容从合成 `block:<id>` 到真实 `cardID` 的映射升级。
- 改进与 Anki 映射的匹配策略，支持同时按 `cardId` / `blockId` 识别同一张卡，降低重复建卡与失联风险。
- 为 SQL 卡片表与闪卡块扫描增加分页读取，规避单次查询结果截断导致的数量异常问题。
- 为删除预览增加可靠性保护：仅在 SQL / 块扫描结果可靠时才允许判定映射已删除，降低误删 Anki 笔记风险。
- 更新并重新打包发布产物。

## 0.1.3

- 重新启用 i18n，并新增 `en_US` / `zh_CN` 语言文件，使插件可随思源界面语言自动切换显示文案。
- 将插件显示英文名调整为 `Anki Linker`，中文名保持为 `思源 Anki 同步工具`。
- 更新面板标题与顶栏悬浮提示，统一使用本地化名称而非项目名 `siyuan-ankiLinker`。
- 从 `keywords` 中移除 `siyuan`，避免关键词包含该词。
- 优化“本地 AnkiConnect”配置区表单布局，使左侧字段标签与右侧下拉/输入控件纵向对齐，提升界面美观度。
- 新增对 `Esc` 键的响应：面板打开时可直接按 `Esc` 关闭插件面板。
- 将插件中文显示名称由“思源 Anki 同步器”调整为“思源 Anki 同步工具”。
- 删除 `plugin.json` 中空的 `funding` 字段。
- 删除与 `default` 完全重复的 `displayName.en_US` 与 `description.en_US`，消除冗余 locale 配置。

## 0.1.2

- 将插件实际 ID 从 `ankiLinker` 统一调整为 `siyuan-ankiLinker`，使手动安装目录名与仓库名保持一致。
- 增加旧插件 ID `ankiLinker` 到新插件 ID `siyuan-ankiLinker` 的本地数据迁移逻辑，尽量避免升级后配置与映射丢失。
- 运行时全局键、顶栏图标标识、面板根节点类名/ID 等内部命名同步切换到 `siyuan-ankiLinker` 风格。
- Anki 标签查询与写入兼容新旧标签，避免既有同步记录失联。
- 卸载时同时尝试清理新旧插件 ID 目录下的持久化数据。

## 0.1.1

- 单独更新并提交 `preview.png`，覆盖远端预览图。
- 新增 `uninstall()` 清理逻辑，完整卸载插件时会删除插件自身创建的持久化数据文件。
- `onunload()` 增加运行时状态清理，避免窗口全局对象残留。
- 更新版本号并重新打包发布产物。

## 0.1.0

- 完善插件元信息：作者、仓库、主页、关键词、描述。
- 项目仓库名更新为 `siyuan-ankiLinker`。
- 插件显示名称调整为更友好的中英文名称。
- 取消发布禁用标记，允许正式发布。
- 新增顶栏自定义同步图标并替换原内置图标。
